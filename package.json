{
  "name": "yakit",
  "private": true,
  "version": "1.0.0",
  "description": "Akaryakit istasyonu self-servis sistemi (kiosk, operator paneli, yonetici paneli, backend).",
  "workspaces": [
    "server",
    "web",
    "agent"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:web": "npm run dev --workspace web",
    "dev:agent": "npm run dev --workspace agent",
    "build": "npm run build --workspace server && npm run build --workspace web",
    "build:server": "npm run build --workspace server",
    "build:web": "npm run build --workspace web",
    "build:agent": "npm run build --workspace agent",
    "start": "npm run start --workspace server",
    "lint": "npm run lint --workspace server && npm run lint --workspace web && npm run lint --workspace agent",
    "typecheck": "npm run typecheck --workspace server && npm run typecheck --workspace web && npm run typecheck --workspace agent",
    "test": "npm run test --workspace server && npm run test --workspace web && npm run test --workspace agent",
    "seed": "npm run seed --workspace server"
  },
  "engines": {
    "node": ">=18.18.0"
  },
  "overrides": {
    "qs": "6.15.3"
  }
}
